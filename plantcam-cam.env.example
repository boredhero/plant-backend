# Per-camera config — copy to /home/noah/plantcam-cam1.env, plantcam-cam2.env, etc.
# Each camera gets its own env file with independent settings.

# Camera identity (USB vendor:model ID — immutable hardware identifier)
# Find with: udevadm info --query=property --name=/dev/videoN | grep ID_VENDOR_ID
CAM_USB_ID=0000:0000
ROCKPRO64_IP=
CAM_PORT=8080

# HLS output directory (each camera needs its own)
HLS_DIR=/var/www/planting.martinospizza.dev/cam/hls

# ffmpeg video filter chain — tune per camera for color correction, LED banding, etc.
# Note: use single % for drawtext (not %% — env files don't need systemd escaping)
VF_FILTERS=lagfun=decay=0.9995,tmedian=radius=3:percentile=1,eq=brightness=-0.12:saturation=1.15:contrast=1.08,colorbalance=rm=0.0:bm=-0.03:rh=-0.05:bh=-0.01,drawtext=text='%{localtime}':fontsize=20:fontcolor=white:borderw=2:bordercolor=black:x=10:y=h-35,format=nv12,hwupload

# Human-readable label (used by frontend)
CAM_LABEL=Camera 1
